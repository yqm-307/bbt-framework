# AGENTS.md — bbt-framework

## 定位与职责

- 本仓是可 fork 的 C++ 正式服务框架与工程脚手架：App/Service 基类、启动/生命周期、配置、注册发现、Stub 等分布式基础能力；使用者在应用层目录开发业务。
- 本仓通过 `bbtools-infra` 的公开构建目标统一获得 HTTP、RPC、MCP 和第三方适配；infra 按需消费 core/coroutine。旧 http/rpc/network/conet 仅为迁移来源，禁止新增直接依赖。
- 框架上游不硬编码具体产品业务；fork 后应用层正是业务开发位置。通用注册发现和 Stub 属于框架/扩展，不与 Workbench 业务任务/租约混淆。不强制其他语言应用或 collab-services 成为本仓 fork。
- 当前仅规范初始化，尚无框架构建/测试入口；不要把目标能力写成现有事实。

## 工作方式

- 跨工具规则以本文件为入口，README 描述产品现状，GitHub Issue/PR 记录任务、决策和验收。
- 读取目标 Issue、实际文件、分支与依赖版本后工作；交付可 fork 后新增业务服务的工程，不以仅跑通示例替代；框架/扩展/应用目录边界是明确需求，具体命名、接口和实现留给后续设计，不预建全部空模块。
- 普通任务最小充分修改，不自动进入 SDD；新增依赖、公共接口破坏、CI/发布流程变更先确认。第三方依赖由 bbtools-infra 统一评估：标准库/Boost 优先，必要新依赖需社区活跃度、架构/平台/许可/安全/体积与退出路径证据，并通过现代 C++ adapter 隔离。
- 每任务一位实现负责人；并行写入用独立工作区。上游契约未确定时不各猜一版，可先做不依赖它的工作；集成固定已验收版本。
- 不复制其他仓库的 SOUL/Skill；本仓指令应可独立读取，不依赖 Alice 聊天历史或本机目录。

## 工程与验证规范

- 文档和交付正文使用中文，标识符、命令与错误原文保留。
- 按实际模块建立代码、测试和可复现构建入口；本轮不锁定后端库、工具链最低版本或新依赖。
- 生命周期、错误和依赖职责在实现中明确，服务关闭不等于底层在途 I/O 已完成。
- 文档修改运行 `git diff --check`，核对路径和命令；实现后 README 必须给出真实运行/测试入口。
- 非平凡逻辑有可运行验证；在干净 fork/等价隔离副本的应用层新增服务，复用 App/Service，验证构建、测试、启动、真实请求/错误/关闭，不为普通业务改核心。完整框架 PRD 另需所选分布式扩展的注册/发现与 Stub 调用链证据；替身不替代承诺的真实集成。
- 本地验证边界：必走冒烟 + 本次开发功能的单测 + 直接耦合功能的单测，三者全过；本地不跑全量测试，全量走 PR 的 CI。同机多 agent 并行时：本地并发取 `JOBS=$(( $(nproc) / 4 ))`（最小2，8核→2），增量用 `--target <t> --parallel $JOBS`；确需本地全量时串行（`flock /tmp/bbt-build.lock`，一次一个，`--parallel $(( $(nproc) / 2 ))`，8核→4）；禁止 bare `--parallel` / bare `ninja` / `make -j$(nproc)`。
- 只存长期规范和必要摘要；构建用已忽略的 build 目录，原始日志、会话、凭据和私有路径不入仓。

## 分层与依赖约束

- 明确框架、扩展、应用及相应配置/测试/构建的职责位置；现在不固定目录名，不把框架核心混在业务目录中。
- 应用依赖框架/所选扩展；扩展实现依赖框架公开扩展接口与 bbtools-infra 能力；核心定义接口但不硬依赖具体扩展。应用/启动组装负责选择与注册，运行时回调不是代码依赖反转。
- 扩展之间依赖必须显式、无环且只用公开契约；扩展/核心不依赖具体业务。bbtools-infra/core/coroutine 可独立消费，不引用 framework 类型。
- RPC 协议/传输归 bbtools-infra，通用 Stub 集成归框架/扩展，业务接口与生成 Stub 归应用。服务注册/发现归框架/扩展使用层，注册中心后端协议/客户端 adapter 归 infra；不默认自研注册中心服务。
- 应用确需直用工具层时声明公开依赖，不绕过生命周期/配置/安全规则。跨仓固定 bbtools-infra 的已验收版本并只链接所需模块；不把 HTTP/RPC/MCP 全部模块设为统一开工前置。
- fork 中的业务改动和框架/通用扩展改动隔离，维护上游修复吸收边界；不保证任意 fork 零冲突升级。目标分层见 README，具体公开契约仍由拥有者维护。

## 权限与交付

- 仅改获授权范围，不覆盖已有改动；经用户授权后 commit/push，代码走 PR，不直推默认分支、不自行合并或发布。
- 涉公共契约、权限与并发的关键改动独立审查；保留仓库既有 required checks，不宣称已配置未实查的远端保护。
- 无授权不改基础设施、不部署、不安装系统依赖、不执行付费调用或破坏性清理。建 Issue 不等于启动实施。
- 返回实际版本、变更、验证与未覆盖项、产物链接和下一步。缺依赖或权限时 BLOCKED，不用 stub 或削弱测试伪装完成。

## 独立接续与同机协作

- 用户可直接与本仓 Agent 确认具体任务，不必经 Alice 转交或审批；跨仓契约变化与相关拥有者协调，不能代改其他仓。用户明确消息或携带其授权范围的任务说明可作为输入，Issue 存在不等于授权；本节不放宽既有提交、依赖和安全门禁。
- 开工在任务说明中记清：目标/非目标、实现负责人、仓库与基线完整 SHA、实际工作区/分支、允许写入、依赖产物及就绪判据、验证和交付位置。可复用 Issue/PR/交接正文，不强制新系统或固定模板。普通可查信息先查；缺方向或权限才问用户。
- 写任务各用独立工作区/分支、build 和依赖目录；不共享 Git 索引、测试数据或未声明的进程。复用上游须核对源码 SHA、模块及实际链接的依赖路径和对应验证；tag 可用但须解析到 SHA，`/usr/local` 已安装不等于版本已固定。不自动系统安装或覆盖共享前缀；现有构建暂不能隔离时先记录阻塞，不擅改构建入口。
- 测试优先动态端口与自有进程；需要固定共享服务时，在任务中声明资源、复用/互斥和清理边界，不终止他人进程。保留已有脏改动；只读不要求全仓洁净，写任务不裹入他人改动。
- 同机新 worktree 和云端 clone 都不会带入未提交规范。开工核对实际拿到的规范版本；未发布时由授权交接者提供明确候选快照/差异及适用范围，不把旧暂存版当最新版。长期结论保留于本仓规定文档/PR，原始 `.state`/会话不作唯一公共依据；不要求读取 Alice 历史或私有记忆。

- 可先确认 App/Service、框架/扩展/应用边界与 fork 使用场景；真实链接/集成只等消费模块的契约、公开构建目标、源码 SHA 和验收证据，不等全部 infra 模块。Stub/codegen 与业务 IDL 的具体边界由首个 RPC/扩展任务明确，不预建生成器平台。
- 协议接入/依赖政策真源归 [bbtools-infra #1](https://github.com/yqm-307/bbtools-infra/issues/1) 及其 `docs/decisions/0001-unified-infra.md`；未发布文件不当可用依赖，使用已交接候选或 Issue 正文。框架扩展接口归本仓，摘要不替代上游契约；无需私有协调总纲才能开发。

## 任务入口

- 当前职责与首阶段 PRD：https://github.com/yqm-307/bbt-framework/issues/1
- 进入后续实施前核对本地与远端规范版本；主 Issue 保持需求/验收状态，不在 AGENTS 记录逐次进度。
